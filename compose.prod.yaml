services:
  app:
    image: ghcr.io/ninjainshade/clash-armies:${APP_VERSION}
    restart: unless-stopped
    depends_on:
      db:
        # Don't start app until db accepts connections
        condition: service_healthy
    env_file: .env
    ports:
      - '127.0.0.1:${HOST_PORT}:3000'
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'
  db:
    image: mariadb:11.3
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MARIADB_USER: ${DB_USER}
      MARIADB_PASSWORD: ${DB_PASSWORD}
      MARIADB_DATABASE: clash-armies
    volumes:
      - clash-armies-db:/var/lib/mysql
    ports:
      # Port only exposed for easier debugging (note it's only exposed on loopback).
      - '127.0.0.1:20001:3306'
    healthcheck:
      test: ['CMD', 'healthcheck.sh', '--connect', '--innodb_initialized']
      interval: 10s
      timeout: 5s
      retries: 10
    logging:
      driver: json-file
      options:
        max-size: '5m'
        max-file: '3'
volumes:
  clash-armies-db:
